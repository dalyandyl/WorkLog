# Electron 镜像已迁移：
# - electron-builder 打包阶段：见 electron-builder.yml 的 electronDownload.mirror
# - npm install 阶段（electron 二进制下载）：由用户环境变量 ELECTRON_MIRROR /
#   ELECTRON_BUILDER_BINARIES_MIRROR 提供（运行 scripts/set-mirrors.ps1 一次性设置）
# 不要在这里写 electron_mirror 等非 npm 官方配置键，否则 npm 会报警告。
