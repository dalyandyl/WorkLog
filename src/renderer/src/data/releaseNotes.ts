/**
 * 更新公告（内置打包数据）
 *
 * 维护方式：每次发布新版本时，把该版本的更新内容作为新条目加在数组最前面，
 * 与 CHANGELOG.md 保持一致（content 即该版本章节的 Markdown 正文）。
 * 版本号格式建议 `x.y.z`（可带 -beta 后缀）；日期为 `YYYY-MM-DD`。
 */
export interface ReleaseNote {
  version: string
  date?: string
  content: string
}

export const RELEASE_NOTES: ReleaseNote[] = [
  {
    version: '1.5.3',
    date: '2026-09-10',
    content: `- 新增「便签」功能：左侧导航「管理 → 便签」，可新增 / 置顶 / 标记完成 / 编辑 / 删除便签
- 置顶且未完成的便签显示在每天日报任务列表上方，直到标记完成前日报都能看到；完成后保留可恢复
- 便签完整参与备份与 WebDAV 同步；便签页为通栏卡片布局 + 概览统计，支持深色模式
- 新增任务「延期」：未完成任务可在日报任务详情或发布页追加自定义新区间（默认今天起、长度与原区间一致）
- 延期后原区间 + 新区间所有日期共享同一任务，任一天完成 / 编辑 / 备注全局同步，重叠日期自动跳过`
  },
  {
    version: '1.5.2',
    date: '2026-09-10',
    content: `- 恢复 GPU 硬件加速，修复按钮悬停、弹窗打开卡顿`
  },
  {
    version: '1.5.1',
    date: '2026-09-09',
    content: `- 新增「更新公告」页，展示每个版本发布的新内容
- 启动时自动检测新版本，右下角气泡提示（下载进度、重启安装）
- 全项目下拉列表统一为报表样式；左侧菜单分组可收起
- 日报任务勾框美化；取消主区底部状态栏
- 更新提示按钮颜色随主题强调色变化`
  }
]
